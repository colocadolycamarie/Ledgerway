import { ArrowRight, ShieldCheck } from 'lucide-react';
import { Link } from 'wouter';
import { Logo } from '@/components/layout/logo';

// TODO: the control-view stats below ("$1.84m of $2.40m", "76%", "Q3 / 2024",
// pending/exception counts, bar heights) are placeholder marketing content,
// not live data. Wire to a real public metrics endpoint or replace with
// static illustrative copy that isn't dressed up as a live view.
export default function Landing() {
  return <div className="landing ledger-grid"><header className="landing-nav"><Logo /><div className="landing-links"><a href="#control">Control chain</a><a href="#teams">For every team</a><Link href="/sign-in" className="landing-signin" data-testid="link-sign-in">Sign in <ArrowRight size={15} /></Link></div></header><section className="landing-hero"><div className="hero-copy"><p className="eyebrow accent-eyebrow">Procurement, with a paper trail</p><h1>Spend moves<br /><em>with intention.</em></h1><p>Ledgerway gives every request a clear route from need to payment — with the evidence finance needs at every handoff.</p><div className="hero-actions"><Link href="/sign-up" className="button button-primary" data-testid="link-get-started">Get started <ArrowRight size={16} /></Link><a href="#control" className="text-link" data-testid="link-see-control">See the control chain <ArrowRight size={15} /></a></div></div><div className="hero-control"><div className="control-window"><div className="window-top"><span><i /> illustrative view</span><span className="font-mono">Sample workspace</span></div><div className="control-head"><div><p className="eyebrow">Commitment overview</p><strong>$1.84m</strong><small>of $2.40m allocated</small></div><span className="ring-progress">76%</span></div><div className="mini-bars"><span style={{ height: '45%' }} /><span style={{ height: '62%' }} /><span style={{ height: '53%' }} /><span style={{ height: '78%' }} /><span style={{ height: '68%' }} /><span style={{ height: '86%' }} /><span style={{ height: '75%' }} /></div><div className="control-row"><span>Pending approvals</span><b>12 <ArrowRight size={13} /></b></div><div className="control-row"><span>Match exceptions</span><b className="warning">3 <ArrowRight size={13} /></b></div></div><div className="hero-stamp"><ShieldCheck size={17} /><span>Evidence, attached<br /><b>at every step.</b></span></div></div></section><section id="control" className="landing-chain"><p className="eyebrow">One continuous record</p><h2>From “we need this”<br />to “paid and posted.”</h2><div className="chain"><div><span>01</span><strong>Request</strong><p>A guided intake that captures the why, not just the what.</p></div><div><span>02</span><strong>Approve</strong><p>Route by policy, with context visible before a decision.</p></div><div><span>03</span><strong>Match</strong><p>PO, receipt, and invoice meet in one auditable view.</p></div><div><span>04</span><strong>Pay</strong><p>Release only what has earned its way through control.</p></div></div></section><section id="teams" className="landing-proof"><div><p className="eyebrow accent-eyebrow">Calm under pressure</p><h2>Precision is a team sport.</h2></div><p>Requesters get speed. Approvers get signal. Finance gets a defensible record. One system, without the handoff tax.</p><Link href="/sign-up" className="button button-dark" data-testid="link-create-workspace">Create your workspace <ArrowRight size={15} /></Link></section><footer className="landing-footer"><Logo /><span>© {new Date().getFullYear()} Ledgerway · Spend with intention.</span><span className="font-mono">CONTROL / CLARITY / CUSTODY</span></footer></div>;
}
